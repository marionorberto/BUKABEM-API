export enum EnumMentorshipStatus {
  PENDIND = 'pendente',
  ACCEPTED = 'aceite',
  CONCLUDE = 'concluída',
}

export enum EnumMentorshipType {
  PROJECT = 'projecto',
  ACADEMIC = 'ACADÉMICA',
  OTHER = 'OUTRO',
}
